# 🧠 Smart Answer - Interview Prep Assistant 🚀

**Smart Answer** เป็นแอปพลิเคชัน Desktop บนระบบปฏิบัติการ Windows ที่ออกแบบมาเพื่อเป็นผู้ช่วยส่วนตัวในการเตรียมตัวสัมภาษณ์งานแบบ Real-time ตัวแอปพลิเคชันสามารถดักจับสัญญาณเสียงของผู้สัมภาษณ์ ไม่ว่าจะเป็นเสียงพูดรอบข้างผ่านไมโครโฟน (Microphone) หรือเสียงสัมภาษณ์ออนไลน์ผ่านโปรแกรมสนทนา (Zoom, MS Teams, Google Meet, Discord ฯลฯ) ผ่าน WASAPI Loopback แล้วทำการแปลงเสียงเป็นข้อความ พร้อมใช้พลังของโมเดล **Google Gemini AI** ในการวิเคราะห์เจตนา คัดกรองเฉพาะคำถาม และแนะนำแนวทางการตอบคำถามสัมภาษณ์งานอย่างมืออาชีพในรูปแบบโครงสร้างเชิงลึก (เช่น STAR Framework) ได้ทันที

---

## ✨ ฟีเจอร์หลัก (Key Features)

1. **🎙️ Dual Audio Input Mode**
   * **Mic Mode**: บันทึกเสียงพูดจากไมโครโฟนภายนอก
   * **System Audio Mode (WASAPI Loopback)**: ดักฟังเสียงที่ดังออกจากลำโพงคอมพิวเตอร์โดยตรง (ผ่าน `PyAudioWPatch`) ช่วยให้ดักจับเสียงของผู้สัมภาษณ์จากแอปพลิเคชันการประชุมออนไลน์ได้อย่างคมชัดและไร้เสียงรบกวนภายนอก
2. **🔇 Real-time VAD & Noise Calibration (Auto-Trigger vs Manual Mode)**
   * **Auto-Trigger (VAD)**: ระบบตรวจจับการพูดอัตโนมัติ เมื่อเริ่มบันทึกจะมีการจูนระดับเกณฑ์ตัดเสียงเงียบ (VAD Threshold Cap) เป็น 0.080 เพื่อลดปัญหากวนใจจากห้องที่มีเสียงพัดลมหรือเครื่องปรับอากาศดัง ระบบจะส่งเสียงไปถอดความทันทีเมื่อหยุดพูดตามเวลาที่กำหนดโดยอัตโนมัติ
   * **Manual Mode**: ระบบบันทึกแบบควบคุมเอง เหมาะสำหรับการเตรียมตัวในห้องที่มีระดับเสียงรบกวนภายนอกสูงมาก หรือกรณีที่ผู้สัมภาษณ์มีจังหวะพูดช้า/มีช่วงหยุดคิดค่อนข้างนาน ป้องกันข้อผิดพลาดจากระบบ Auto-Trigger ที่อาจตัดเสียงและส่งประมวลผลก่อนพูดจบ
3. **🎵 Scipy Spectral Noise Reduction**
   * ประมวลผลเสียงด้วยอัลกอริทึม STFT Spectral Subtraction ผ่าน Scipy เพื่อกรองเสียงรบกวนพื้นหลังและเสียงฮัมจากอุปกรณ์เสียงออกก่อนส่งไปถอดความ
4. **💬 Live Speech Captioning**
   * แสดงผลข้อความถอดความคำพูดของผู้พูดแบบสดๆ บนหน้าจอขณะกำลังพูดผ่าน Google Speech Recognition API
5. **🤖 AI Analysis Engine & Resume Integration**
   * **Google Gemini AI SDK**: ส่งคำถามสัมภาษณ์ไปวิเคราะห์คัดกรองเจตนาเพื่อวิเคราะห์คำถามจริง
   * **Custom OpenAI-Compatible Provider**: รองรับการใช้งาน API ภายนอกอื่นๆ หรือรันโมเดลบนเครื่องตนเอง (เช่น Ollama, DeepSeek, Local AI, OpenAI API) โดยตั้งค่า API Base URL, Custom API Key และชื่อโมเดลได้ตามใจชอบ
   * **Resume-Based Context**: ระบบแนะนำคำตอบเฉพาะบุคคล โดยผู้ใช้สามารถอัปโหลดเรซูเม่ (.pdf, .txt) หรือวางข้อความรายละเอียดประวัติส่วนตัว เพื่อให้ AI นำมาใช้วิเคราะห์และปรับเปลี่ยนทักษะรวมถึงตัวอย่างการเล่าเรื่องในแนวทางการตอบคำถามสัมภาษณ์ (STAR Framework) ให้เหมาะสมและเป็นตัวคุณมากที่สุด
   * วิเคราะห์ข้อมูลแบบจำแนกโครงสร้าง (Structured JSON Output via Pydantic Schema) เพื่อดึงคำตอบในมิติต่างๆ:
     * **Category**: หมวดหมู่คำถาม (Soft Skills, Technical, Behavioral, OOP, React, ฯลฯ)
     * **Focus Areas**: จุดประสงค์ที่ผู้สัมภาษณ์ใช้ถามเพื่อวัดผลผู้สมัคร
     * **Key Points**: ประเด็นสำคัญที่ควรหยิบยกมาตอบแบบย่อ
     * **STAR Framework**: โครงสร้างแนวคิดแนวทางการตอบ (Situation, Task, Action, Result) สำหรับคำถามเชิงพฤติกรรม
     * **Answer Strategy & Warnings**: กลยุทธ์และข้อควรระวังในการตอบ
     * **Example Outline**: ลำดับการเล่าเรื่องเป็นขั้นเป็นตอน
     * **Suggested Answer**: ตัวอย่างคำตอบแบบสมบูรณ์และเป็นภาษาเขียนระดับมืออาชีพในภาษาไทย
6. **📱 Mini Mode (ปรับความโปร่งใสหน้าต่างและย่อขยายได้)**
   * หน้าต่างลอยขนาดกะทัดรัด (เริ่มต้น 470x450 พิกเซล) และปักหมุดไว้ด้านบนสุดเสมอ (Always on Top)
   * แถบปรับความโปร่งใส (Opacity Slider) ตั้งแต่ 20% ถึง 100% ป้องกันการบดบังเนื้อหาหรือตัวนำเสนออื่นๆ ในหน้าต่างเบื้องหลัง
   * หน้าต่างสามารถย่อขยายขนาดได้อย่างอิสระ (Resizable) พร้อมระบบจัดระเบียบเนื้อหาและปัดบรรทัดข้อความอัตโนมัติ (Auto-wrap)
7. **📂 History & Configuration System**
   * บันทึกคำถามและการวิเคราะห์ย้อนหลังในรูปแบบ Local JSON จัดหมวดหมู่ประวัติคำถาม และสามารถกลับมาเรียกดูหรือลบข้อมูลที่ไม่ต้องการออกได้ในภายหลัง

---

## 🛠️ โครงสร้างสถาปัตยกรรม (Project Structure)

```bash
smart_answer/
│
├── app.py                   # ไฟล์หลักสำหรับรันแอปพลิเคชันและจัดการ GUI (SmartAnswerApp)
├── config.py                # จัดการการอ่าน/เขียน config.json และการเก็บประวัติ History
├── config.example.json      # แม่แบบไฟล์ค่าตั้งต้นสำหรับสร้าง config.json
├── gemini_client.py         # จัดการการประมวลผลผ่าน Google Gemini API ด้วย Structured Output Schema
├── transcriber.py           # ตัวถอดความเสียงด้วย Google Speech API (WhisperTranscriber Wrapper)
├── requirements.txt         # ไฟล์ระบุ Dependencies ทั้งหมดของโครงการ
│
├── audio/                   # โมดูลย่อยสำหรับการบันทึกและประมวลผลเสียง
│   ├── __init__.py
│   ├── recorder.py          # คลาสควบคุมการอัดเสียง (sounddevice / PyAudioWPatch), VAD และ Calibration
│   └── processing.py        # ฟังก์ชันคำนวณและประมวลผลไฟล์เสียง (Resampling & Noise Reduction)
│
├── ui/                      # โมดูลย่อยสำหรับ UI หน้าต่างแอปพลิเคชัน
│   ├── __init__.py
│   ├── navigation.py        # หน้าเมนูด้านซ้ายสำหรับนำทางสลับหน้าต่างๆ
│   ├── dashboard_page.py    # หน้าหลักสำหรับบันทึกเสียงและแสดงการ์ดผลลัพธ์คำแนะนำ
│   ├── history_page.py      # หน้าสำหรับย้อนอ่านประวัติคำถามการตอบ
│   ├── settings_page.py     # หน้าสำหรับตั้งค่า API Key, รุ่นของโมเดล, เลือกอุปกรณ์ และการเชื่อมต่อเสียง
│   ├── coaching_card.py     # ส่วนประกอบ UI แสดงโครงสร้างคำแนะนำแบบยืดหดขยายได้ (STAR Framework)
│   └── svg_images.py        # รูปภาพ SVG ในรูปแบบ Raw Text สำหรับความรวดเร็วในการเปิดและย่อหน้าต่าง
│
└── [Scripts]                # สคริปต์ทดสอบสำหรับการพัฒนาและการดีบั๊กปัญหา
    ├── list_devices.py      # ตรวจสอบและแสดงรายชื่ออินพุตเสียงและลูปแบคทั้งหมดในระบบ
    ├── test_audio.py        # ทดสอบการอัดเสียงพื้นฐานของไมโครโฟน
    ├── test_wasapi.py       # ตรวจสอบว่าระบบ Windows WASAPI Loopback ใช้งานได้ปกติหรือไม่
    ├── test_stereo_mix.py   # ตรวจสอบความถูกต้องของการใช้งาน Stereo Mix
    ├── test_gemini.py       # ทดสอบการส่งข้อความคำถามเพื่อรับการวิเคราะห์จาก Gemini API
    ├── test_whisper.py      # ทดสอบการเรียกใช้งาน Google Speech Recognition
    └── generate_ico.py      # สคริปต์สำหรับแปลงรูปภาพเป็นไฟล์ app_icon.ico
```

---

## 🚀 วิธีการติดตั้งและใช้งาน (Installation & Quickstart)

### 📋 สิ่งที่ต้องเตรียมก่อนเริ่มใช้งาน
* **Python** เวอร์ชั่น 3.9 ขึ้นไป
* ระบบปฏิบัติการ **Windows** (เพื่อรองรับการใช้งาน WASAPI loopback สำหรับอัดเสียงระบบ)
* **API Key ของ Gemini** (สมัครใช้งานฟรีได้ที่ [Google AI Studio](https://aistudio.google.com/)) หรือ OpenAI-Compatible API Key/Endpoint

### 📦 ขั้นตอนการติดตั้ง

1. **ดาวน์โหลดโปรเจกต์** ลงบนเครื่องของคุณ
2. **สร้าง Virtual Environment** (แนะนำ) และเปิดใช้งาน:
   ```powershell
   python -m venv .venv
   .venv\Scripts\activate
   ```
3. **ติดตั้งไลบรารีที่จำเป็น** ผ่าน `requirements.txt` (รวมถึง `pypdf` สำหรับรองรับการอ่านเรซูเม่สกุล PDF):
   ```bash
   pip install -r requirements.txt
   ```
4. **เปิดสิทธิ์การบันทึกเสียงลำโพงคอมพิวเตอร์ (สำหรับ System Audio Loopback)**
   * คลิกขวาที่ไอคอนลำโพงบริเวณมุมขวาล่างของแถบงาน Windows -> เลือก **Sound settings**
   * ไปที่ **More sound settings** -> แท็บ **Recording**
   * ตรวจสอบว่าเปิดใช้งานอุปกรณ์ **Stereo Mix** หรืออุปกรณ์ Output เริ่มต้นของคุณแล้ว เพื่อความเสถียรในการทำงานของ WASAPI Loopback บนระบบปฏิบัติการ Windows

---

## 💻 วิธีการรันโปรแกรม

รันไฟล์ `app.py` ด้วยคำสั่ง:
```bash
python app.py
```

### ⚙️ ขั้นตอนการเริ่มใช้งานครั้งแรก
1. เมื่อเปิดหน้าจอโปรแกรมขึ้นมาเป็นครั้งแรก หากยังไม่มี API Key ระบบจะนำคุณไปยังหน้า **Settings** โดยอัตโนมัติ
2. เลือกว่าจะใช้งาน **Google Gemini** หรือ **Custom (OpenAI)**:
   * **Google Gemini**: กรอก Gemini API Key และเลือกโมเดล (แนะนำ `gemini-3.1-flash-lite`)
   * **Custom (OpenAI)**: ระบุ API Base URL, Custom API Key และชื่อโมเดล (เช่น หากใช้ Ollama ให้ระบุ `http://localhost:11434/v1` และระบุชื่อโมเดลที่ต้องการเรียกใช้งาน)
3. เลือกรูปแบบ **Audio Source Type** ว่าจะใช้ไมโครโฟนของคุณ (`mic`) หรือจับเสียงคอมพิวเตอร์สัมภาษณ์ออนไลน์ (`system`)
4. **(ไม่บังคับ)** ที่การ์ด **Resume & Profile**:
   * เปิดสวิตช์ใช้งานข้อมูลเรซูเม่ (Use Resume Context)
   * กดปุ่ม **📁 อัปโหลดไฟล์ (.pdf, .txt)** เพื่อเลือกไฟล์เรซูเม่ประวัติการทำงานของคุณ (ใช้ไลบรารี `pypdf` ในการอ่านข้อมูล) หรือวางข้อมูลประวัติส่วนตัวลงในช่องรับข้อมูลโดยตรง
5. กดปุ่ม **Test API Key** เพื่อทดสอบเชื่อมต่อและรับส่งข้อมูล
6. สลับกลับมาที่หน้า **Dashboard**:
   * เลือกโหมดการตรวจจับเสียงตามความต้องการ: **Auto-Trigger** (ระบบจูนเกณฑ์เสียงเงียบอัตโนมัติ) หรือโหมด **Manual** (ควบคุมอัด/ส่งวิเคราะห์ด้วยตนเองผ่านปุ่มหรือปุ่มลัด Spacebar)
   * กดปุ่ม **Start Listening** และเริ่มกระบวนการซ้อมสัมภาษณ์งานได้ทันที!


---

## 📦 วิธีการสร้างไฟล์ Executable (.exe)

คุณสามารถแปลงแอปพลิเคชันนี้ให้เป็นไฟล์ `.exe` สำหรับใช้งานบน Windows ได้โดยใช้ **PyInstaller** ด้วยขั้นตอนดังนี้:

1. **ติดตั้ง PyInstaller** ใน Virtual Environment:
   ```bash
   pip install pyinstaller
   ```
2. **สร้างไฟล์ไอคอนของแอปพลิเคชัน (`app_icon.ico`)** โดยการรันสคริปต์เพื่อแปลงโลโก้สมองแบบ SVG เป็นไอคอนระบบ Windows:
   ```bash
   python generate_ico.py
   ```
3. **คอมไพล์โปรแกรมด้วย PyInstaller**:
   * **วิธีที่ 1 (แบบบรรทัดเดียว - แนะนำ)**: สั่งคอมไพล์โปรแกรมแบบไฟล์เดี่ยว (Single File) พร้อมซ่อนหน้าต่าง Console, ใส่ไอคอน และดึงธีม UI CustomTkinter พร้อมตัดโมเดล Whisper ออกเพื่อความกะทัดรัด:
     ```bash
     .venv\Scripts\pyinstaller -F -w --icon=app_icon.ico --collect-all customtkinter --exclude-module faster_whisper app.py
     ```
   * **วิธีที่ 2 (แบบอ้างอิง Spec File)**: คอมไพล์ผ่านไฟล์ตั้งค่า `app.spec` ที่มากับโครงการ:
     ```bash
     pyinstaller app.spec
     ```
4. เมื่อกระบวนการสร้างเสร็จสิ้น คุณจะพบไฟล์แอปพลิเคชัน `.exe` ที่พร้อมใช้งานภายในโฟลเดอร์:
   `dist/app.exe`

---

## 🛠️ เครื่องมือทดสอบสำหรับผู้พัฒนา (Developer Testing Scripts)

ในโฟลเดอร์โครงการ มีสคริปต์ที่นักพัฒนาหรือผู้ใช้สามารถเรียกใช้เพื่อตรวจสอบการทำงานของฮาร์ดแวร์เสียงหรือ API ได้อย่างอิสระ:

* **ตรวจสอบอุปกรณ์เสียงภายในเครื่อง**:
  ```bash
  python list_devices.py
  ```
  สคริปต์นี้จะแสดงรายการชื่อและเลข Index ของ Input Device รวมถึง WASAPI Loopback ทั้งหมด เพื่อนำไปดีบั๊กจับคู่ในระบบเสียง
* **ทดสอบระบบจำเสียงพูดของไมโครโฟน**:
  ```bash
  python test_audio.py
  ```
* **ทดสอบการเชื่อมต่อ API ของ Gemini**:
  ```bash
  python test_gemini.py
  ```
* **ทดสอบการดักจับสัญญาณเสียงลำโพง (WASAPI Loopback)**:
  ```bash
  python test_wasapi.py
  ```

---

## 🎨 รายละเอียดเทคโนโลยีของแอปพลิเคชัน (Tech Stack)

* **GUI Framework**: [CustomTkinter](https://github.com/TomSchimansky/CustomTkinter) เพื่อรังสรรค์แอปพลิเคชัน Desktop สไตล์ Modern Dark Mode สวยงาม ทันสมัย และลื่นไหล
* **AI API Integration**: `google-genai` SDK เวอร์ชั่นล่าสุด เพื่อความเร็วและความแม่นยำในการเรียกใช้โครงสร้างข้อมูล JSON แบบ Structured Schema
* **Speech to Text**: `SpeechRecognition` ร่วมกับ Google Web Speech API เพื่อการแปลงข้อมูลเสียงพูดสดเป็นตัวหนังสือแบบไม่ต้องใช้ทรัพยากรบนเครื่องสูง
* **Audio Capture API**: `PyAudioWPatch` + `sounddevice` เพื่อให้โปรแกรมดึงคลื่นเสียงจากระบบได้โดยตรงโดยไม่เกิดการดีเลย์ หรือกินกำลังหน่วยประมวลผล (CPU)
* **Digital Signal Processing**: `scipy` (STFT/ISTFT) + `numpy` ในการแปลงสัญญาณเชิงเลขและลดสัญญาณรบกวนรอบตัว

---

## 📄 ใบอนุญาตใช้งาน (License)

โครงการนี้สร้างขึ้นเพื่อจุดประสงค์ในการเป็นเครื่องมือช่วยอำนวยความสะดวกในการซ้อมสัมภาษณ์งานและการพัฒนาเครื่องมือประมวลผลเสียงแบบบูรณาการ ขอสงวนสิทธิ์และให้ใช้งานตามหลักจริยธรรมของนักพัฒนาซอฟต์แวร์
